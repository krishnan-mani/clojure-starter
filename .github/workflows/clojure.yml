name: Clojure CI

on:
  push:
    branches: [ setup ]
  pull_request:
    branches: [ setup ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: lein deps
    - name: Run tests
      run: lein kaocha unit
